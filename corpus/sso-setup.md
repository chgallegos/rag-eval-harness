# Setting Up Single Sign-On

SSO is available on the enterprise tier. We support SAML 2.0 and OIDC.
To enable SSO, an admin must upload the identity provider metadata XML in
Settings, then verify the domain by adding a TXT record.

Once SSO is enforced, password sign-in is disabled for all users in the
organization except break-glass admin accounts.
